'use strict';

var TextureStyle = require('../../../rendering/renderers/shared/texture/TextureStyle.js');
var warn = require('../../../utils/logging/warn.js');

"use strict";
let warned = false;
function warnIgnoredTextureStyle(textureStyle) {
  if (warned) return;
  const scaleModeOnly = new TextureStyle.TextureStyle({ scaleMode: textureStyle.scaleMode });
  if (textureStyle._resourceId === scaleModeOnly._resourceId) return;
  warned = true;
  warn.warn("Text textureStyle: only scaleMode is applied to a text texture, the other fields are ignored");
}

exports.warnIgnoredTextureStyle = warnIgnoredTextureStyle;
//# sourceMappingURL=warnIgnoredTextureStyle.js.map
